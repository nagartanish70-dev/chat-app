[build]
  command = "echo 'No build needed for static site'"
  publish = "."

[context.production]
  environment = { API_URL = "YOUR_BACKEND_URL_HERE" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
